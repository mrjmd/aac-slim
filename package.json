{
  "name": "aac-middleware",
  "version": "0.1.0",
  "private": true,
  "description": "Lightweight middleware for business system integrations",
  "scripts": {
    "dev": "vercel dev",
    "lint": "eslint src/**/*.ts",
    "test": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@upstash/redis": "^1.28.0",
    "libphonenumber-js": "^1.10.54"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "@vercel/node": "^3.0.0",
    "typescript": "^5.3.0",
    "vitest": "^1.0.0"
  },
  "engines": {
    "node": ">=18"
  }
}
