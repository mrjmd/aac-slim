/** @type {import('next').NextConfig} */
const nextConfig = {
  // Keep existing /api folder as standalone Vercel serverless functions
  // Next.js will handle /app routes
}

module.exports = nextConfig
